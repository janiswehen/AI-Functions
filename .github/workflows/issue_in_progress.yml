name: Transfer Labels

on:
  create:
    ref_type:
      - branch

jobs:
  transfer-labels:
    name: Transfer labels from Issue to PR
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repo
      uses: actions/checkout@v2
      
    - name: Install jq
      run: echo test
